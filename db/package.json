{
  "name": "db",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "docker run -e POSTGRES_PASSWORD=password -p 5432:5432 postgres",
    "migrate": "DATABASE_URL=\"postgres://postgres:password@localhost:5432\" prisma migrate dev",
    "client": "cp -r node_modules/.prisma ../api/node_modules/.prisma"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^3.13.0",
    "prisma": "^3.13.0"
  }
}